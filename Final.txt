ALTER TABLE public.derivatives_operations
ADD COLUMN IF NOT EXISTS language VARCHAR(50),
ADD COLUMN IF NOT EXISTS is_complaint BOOLEAN DEFAULT false,
ADD COLUMN IF NOT EXISTS sla_due_at TIMESTAMP;

ALTER TABLE public.treasury_documentation
ADD COLUMN IF NOT EXISTS language VARCHAR(50),
ADD COLUMN IF NOT EXISTS is_complaint BOOLEAN DEFAULT false,
ADD COLUMN IF NOT EXISTS sla_due_at TIMESTAMP;




"language": "",
"is_complaint": false




-- Add these columns to both insert queries
language, is_complaint
-- And their params
'{{this.params.language}}',
'{{this.params.is_complaint}}'










language:     payload.language     || 'English',
is_complaint: payload.is_complaint || false


